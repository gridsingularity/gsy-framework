coverage:
  status:
    project:
      default:
        target: auto  # Compare coverage to the previous base commit (PR base or parent commit)
        informational: false  # If true, status will always pass

ignore:
  - "tests/**/*"  # Do not consider tests when computing codecov coverage

comment:
  layout: "diff"
  behavior: default
